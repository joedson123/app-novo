# Controle de Vendas — corrigido DuplicateWidgetID
- Força banco em `/mount/tmp` no Streamlit Cloud.
- Chaves únicas para todos widgets repetidos.
- Local persistente: `DB_DIR=./data streamlit run app.py`.
